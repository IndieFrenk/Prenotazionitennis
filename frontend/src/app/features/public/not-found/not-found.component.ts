/**
 * Re-export the NotFoundComponent from its shared location so the
 * lazy-loaded route in app.routes.ts can resolve it.
 */
export { NotFoundComponent } from '@app/shared/components/not-found/not-found.component';
